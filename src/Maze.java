public class Maze {
    public Maze(int cols, int rows) {
        
        //We generate the nodes
        for(int i = 0; i < cols; i++) {
            for(int j = 0; j < rows; j++) {
                Node n = new Node(); 
            }
        }
    }
}
